using UnityEngine;
namespace Coop 
{
  public class Teleportable : MonoBehaviour 
  {
      // TODO: Add functionality for following customization events
      // [SerializeField, Tooltip("Event triggered when primary weapon projectile hits this object.")]
      // private ProjectileEvent OnTargetedForTeleport = new ProjectileEvent();
      // [SerializeField, Tooltip("Event triggered just before teleport to a new location has been completed.")]
      // private ProjectileEvent OnTeleporting = new ProjectileEvent();
      // [SerializeField, Tooltip("Event triggered after teleport to a new location has been completed.")]
      // private ProjectileEvent OnTeleported = new ProjectileEvent();
  }
}